<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Dialer" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/system_blue</item>
        <item name="colorPrimaryVariant">@color/system_blue_mid</item>
        <item name="colorOnPrimary">@color/primary_background</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/main_max</item>
        <item name="colorSecondaryVariant">@color/main_max</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.Dialer.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.Dialer.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.Dialer.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>